---
sidebar_label: Vue SDK
---

import CodeBlock from '@theme/CodeBlock';

import mainTs from '!!raw-loader!../../../../../client/web_babylon_js/src/main.ts';

# Vue

## Provider Setup

<CodeBlock language="typescript" title="main.ts">
  {mainTs}
</CodeBlock>

## Composables

### useVircadiaAsset_Vue.ts

#### Usage

{/* Example usage will be given from the client, where we split it out into a separate minimal component to make it clear how it works. */}

### useEntity.ts

#### Usage

{/* Example usage will be given from the client, where we split it out into a separate minimal component to make it clear how it works. */}

