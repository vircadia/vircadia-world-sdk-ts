{
    "tasks": {
        "dev": "deno run --allow-net --allow-read --allow-write --allow-env --import-map=import_map.json dev.ts",
        "build": "deno bundle client.ts dist/bundle.js",
        "lint": "deno lint"
    }
}
