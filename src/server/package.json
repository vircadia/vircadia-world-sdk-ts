{
  "name": "vircadia-world-server",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "tsx watch server",
    "build": "esbuild server --bundle --packages=external --platform=node --format=esm --outdir=dist --sourcemap"
  },
  "dependencies": {
    "@types/socket.io": "^3.0.2",
    "express": "^4.19.0",
    "peer": "^1.0.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.13.0",
    "esbuild": "^0.21.0",
    "tsx": "^4.0.0"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
